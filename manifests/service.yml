apiVersion: v1
kind: Service
metadata:
    name: "parteicluster-db10"
    labels:
        app: "parteicluster-db10"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "parteicluster-db10"